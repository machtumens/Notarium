{
  "name": "notarium-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:wrangler": "wrangler dev",
    "start": "wrangler dev",
    "build": "tsc",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 execute notarium-db --file=migrations/0001_allow_null_class.sql && wrangler d1 execute notarium-db --file=migrations/0002_add_suspension_fields.sql && wrangler d1 execute notarium-db --file=migrations/0003_add_warning_system.sql",
    "migrate:local": "wrangler d1 execute notarium-db-local --local --file=migrations/0001_allow_null_class.sql && wrangler d1 execute notarium-db-local --local --file=migrations/0002_add_suspension_fields.sql && wrangler d1 execute notarium-db-local --local --file=migrations/0003_add_warning_system.sql",
    "migrate:suspension": "wrangler d1 execute notarium-db --file=migrations/0002_add_suspension_fields.sql",
    "migrate:warning": "wrangler d1 execute notarium-db --file=migrations/0003_add_warning_system.sql",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jose": "^5.2.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.15.0",
    "@cloudflare/workers-types": "^4.20240604.0",
    "@eslint/js": "^10.0.1",
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20.10.0",
    "eslint": "^10.2.1",
    "globals": "^17.5.0",
    "typescript": "^5.3.3",
    "typescript-eslint": "^8.59.0",
    "vitest": "^4.1.5",
    "wrangler": "^4.46.0"
  }
}
